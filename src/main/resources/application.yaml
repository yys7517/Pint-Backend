spring:
  application:
    name: pint-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/pint_db
    username: jskt
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB