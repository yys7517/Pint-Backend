package com.example.pintbackend.service.imageservice;


// auto generates constructor, getters, toString, equals.
public record ImageMetadata(
        Long width,
        Long height,
        String camera
) {
}
